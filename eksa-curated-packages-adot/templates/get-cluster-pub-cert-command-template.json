{
    "Parameters": {
        "commands": [
            "su ssm-user --shell bash -c 'set -o pipefail; export EKSA_CLUSTER_NAME={{EKSA_CLUSTER_NAME}}; export KUBECONFIG=${HOME}/${EKSA_CLUSTER_NAME}/${EKSA_CLUSTER_NAME}-eks-a-cluster.kubeconfig; kubectl get secret sample-cluster-sa -n eksa-system -o jsonpath={.data.tls\\\\.crt} | base64 --decode'"
        ]
    }
}