{
    "Parameters": {
        "commands": [
            "su ssm-user --shell bash -c 'export KUBECONFIG=/home/ssm-user/{{EKSA_CLUSTER_NAME}}/{{EKSA_CLUSTER_NAME}}-eks-a-cluster.kubeconfig; cd /home/ssm-user; set -o pipefail; eksctl anywhere delete package {{PACKAGE_NAME}} --cluster {{EKSA_CLUSTER_NAME}}'"
        ]
    }
}